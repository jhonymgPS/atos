import { RowShadowDirective } from './row-shadow.directive';

describe('RowShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new RowShadowDirective();
    expect(directive).toBeTruthy();
  });
});
